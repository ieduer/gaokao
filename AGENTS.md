# AGENTS.md for AI 高考助手

This project inherits `/Users/ylsuen/CF/AGENTS.md`.

- Canonical source: `/Users/ylsuen/CF/gaokao`; isolated release worktrees are allowed only when
  based on the exact pushed `ieduer/gaokao` commit.
- Production is Cloudflare Pages project `gaokao`, custom domain `gk.bdfz.net`.
- Preserve unrelated canonical dirty work. Never deploy from a dirty or unpushed checkout.
- The 2026 paper importer may read only the hash-bound GKS Beijing manifest and structured
  components. National, other-province, mock, prediction, sample and memory-only papers are out of scope.
- Update `docs/OPERATIONS.md`, `PROJECT_STATE.md` and the workspace action log for every release.
